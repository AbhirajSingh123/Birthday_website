// Central place for table/bucket names so nothing is scattered/hardcoded.
export const TABLES = {
  guests: 'guests',
  wishes: 'wishes',
  eventSettings: 'event_settings',
} as const

export const STORAGE_BUCKETS = {
  profilePhotos: 'profile-photos',
  gallery: 'gallery',
  music: 'music',
} as const
